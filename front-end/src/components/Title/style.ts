import styled from 'styled-components'
import theme from '../../styles/theme'

export const Content = styled.h1`
  font-size: ${theme.sizes.titleFontSize} !important;
  font-weight: 500;
  text-align: center;
  text-transform: uppercase;
  padding: 0 0 20px 0;
`
